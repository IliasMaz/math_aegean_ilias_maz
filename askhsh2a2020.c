#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#math.h
#define e 2.718

int main() {
    int i;

    if ((i>0)&&(i<=2)){
        s=0;
        term=1;
        n=0;
        while(term>e){
            s=s+term;
            n=n+1;
            a=pow(n,x);
            b=n*sin(x);
            term=a/exp(b);
        }
    }





}
